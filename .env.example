# 数据库连接（Neon Postgres）
# Vercel 部署：在 Vercel Marketplace 添加 Neon 集成后自动注入，无需手动配置
# 本地开发如需连接真实数据库，先执行:
#   vercel link
#   vercel env pull .env.local
# 然后取消下面注释（值以 .env.local 拉取结果为准）
#
# DATABASE_URL=postgresql://user:password@ep-xxx.region.aws.neon.tech/neondb?sslmode=require
